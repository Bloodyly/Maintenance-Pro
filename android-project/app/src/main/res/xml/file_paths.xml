<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- AppUpdateInstaller downloads into getExternalFilesDir(null), i.e. the
         app-specific external storage root: matches DownloadManager's
         destination and this FileProvider path exactly. -->
    <external-files-path name="updates" path="." />
</paths>
